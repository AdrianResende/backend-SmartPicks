# Configuração do Banco de Dados PostgreSQL
# Para desenvolvimento local, use a URL do seu PostgreSQL local
# Para produção (Neon), use a URL fornecida pelo Neon

# Exemplo para PostgreSQL local:
DATABASE_URL=postgres://postgres:1234@localhost:5432/SmartPicks?sslmode=disable

# Exemplo para Neon (produção):
# DATABASE_URL=postgres://user:password@ep-xxxxx.region.aws.neon.tech/smartpicks?sslmode=require

# Configurações AWS S3
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_BUCKET_NAME=your_bucket_name

# Porta da aplicação
PORT=8080
